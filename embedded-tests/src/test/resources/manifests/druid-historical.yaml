apiVersion: apps/v1
kind: Deployment
metadata:
  name: druid-historical
  namespace: druid
spec:
  replicas: 1
  selector:
    matchLabels:
      app: druid-historical
  template:
    metadata:
      labels:
        app: druid-historical
    spec:
      containers:
        - name: druid-historical
          image: apache/druid:33.0.0
          args: ["historical"]
          ports:
            - containerPort: 8083
          env:
            - name: DRUID_SET_HOST_IP
              value: "0"
            - name: DRUID_SET_HOST
              value: "0"
            - name: DRUID_XMS
              value: "128m"
            - name: DRUID_XMX
              value: "128m"
          volumeMounts:
            - name: druid-common
              mountPath: /opt/druid/conf/druid/cluster/_common/common.runtime.properties
              subPath: common.runtime.properties
            - name: druid-common
              mountPath: /opt/druid/conf/druid/cluster/data/historical/runtime.properties
              subPath: historical.runtime.properties
      volumes:
        - name: druid-common
          configMap:
            name: druid-common
---
apiVersion: v1
kind: Service
metadata:
  name: druid-historical
  namespace: druid
spec:
  selector:
    app: druid-historical
  ports:
    - port: 8083
